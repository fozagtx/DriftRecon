import { json } from '@sveltejs/kit';import {emptySnapshot,importUploads,snapshot} from '@/lib/app/actions';import type {Source} from '@/lib/types';
type Kind=Source|'groundTruth';
function truth(text:string){try{const p:unknown=JSON.parse(text);return Array.isArray(p)&&p.length>0&&typeof p[0]==='object'&&p[0]!==null&&'fromEventId' in p[0]}catch{return false}}
function classify(name:string,text:string):Kind{const n=name.toLowerCase(),t=text.trim();if(n.endsWith('.json')||t.startsWith('{')||t.startsWith('['))return n.includes('truth')||truth(t)?'groundTruth':'dodo';if(n.includes('gumroad'))return'gumroad';if(n.includes('bank')||n.includes('chase'))return'bank';if(n.includes('stripe'))return'stripe';const h=(text.split(/\r?\n/,1)[0]??'').toLowerCase();if(h.includes('sale_id')||h.includes('email'))return'gumroad';if(h.includes('reference')||(h.includes('date')&&!h.includes('type')))return'bank';return'stripe'}
export async function POST({request}){try{const form=await request.formData();const uploads:Partial<Record<Kind,string>>={};for(const value of form.getAll('files')){if(!(value instanceof File)||!value.size)continue;const text=await value.text();uploads[classify(value.name,text)]=text}await importUploads(uploads);return json(await snapshot())}catch(e){return json(emptySnapshot(e instanceof Error?e.message:'Import failed'),{status:400})}}
